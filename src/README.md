# Redux

## 1 Redux Structure

```
├── applyMiddleware.js      // 中间件管家
├── bindActionCreators.js   // 绑定 dispatch 与 actionCreator 的调用关系
├── combineReducers.js      // 组合 reducer
├── compose.js              // 生成高阶函数工具
├── createStore.js          // 创建 stroe 逻辑
├── index.js                // 入口文件
└── utils
    └── warning.js          // 抛出异常的封装
```
